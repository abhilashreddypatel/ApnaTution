{
    "version": 2,
    "buildCommand": "npm install --prefix frontend && npm run build --prefix frontend",
    "outputDirectory": "frontend/dist/apna-tution-frontend/browser",
    "cleanUrls": true,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}